{
  "_comment": "Merge this 'hooks' key into the target project's .claude/settings.json (create the file if it doesn't exist; if hooks.Stop already has entries, append this entry to the array instead of overwriting it). Installed automatically by SKILL.md Step 1.",
  "hooks": {
    "Stop": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PROJECT_DIR}/.claude/hooks/emap_stop_gate.py\""
          }
        ]
      }
    ]
  }
}
